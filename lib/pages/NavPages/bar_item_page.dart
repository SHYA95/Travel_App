import 'package:flutter/cupertino.dart';

class barItemPage extends StatelessWidget {
  const barItemPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Bar page")));
  }
}
